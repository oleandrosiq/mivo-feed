authController:
  handler: src/modules/auth/handler.handler
  events:
    - httpApi:
        path: /auth/sign-in
        method: POST
    - httpApi:
        path: /auth/confirm
        method: POST
    - httpApi:
        path: /auth/sign-up
        method: POST
